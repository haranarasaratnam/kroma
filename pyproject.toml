[project]
name = "kroma"
version = "0.1.0"
description = "KROMA -- personal STEM tutoring reference site (Flask)."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "Flask>=3.0,<4.0",
    "markdown>=3.6,<4.0",
]
